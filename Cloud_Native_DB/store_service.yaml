apiVersion: v1
kind: Service
metadata:
  name: store-service
spec:
  type: NodePort
  selector:
    app: store-service
  ports: 
    - protocol: TCP
      nodePort: 30000
      port: 80
      targetPort: 3000