# Cloud Native Storage 

Cloud-Native Storage (CNS) refers to storage solutions that are designed specifically for cloud-native applications, which typically run in containerized environments like Kubernetes. CNS is dynamic, scalable, API-driven, and integrates tightly with container orchestrators.