#### Vertex AI Agent Builder 
for search and recommendation you can create generic, media, retail and healthcare apps and data stores.


You can index and search a publicly-accessible website, And you can create a data store based on certain data types: Either with structured data that is organized in a specific format and follows a schema, such as BigQuery, JSON, a provider directory, and product catalogs, Or you can use unstructured data.

