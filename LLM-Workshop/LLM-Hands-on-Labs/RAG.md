# Retrieval Augumented Generation 

1. RAG grants LLM's access to external knowledge sources. 
2. Reduces hallucinations and enables access to its own sources. 
3. Relies on embeddings, which capture relationships between parts of text. 
4. Uses semantic search to summarize and answer questions. 